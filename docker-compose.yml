version: '3.8'  # <--- THIS LINE MUST BE AT THE TOP

services:
  web-app:
    image: nginx:alpine
    networks:
      - site-net

  proxy:
    image: custom-nginx:latest
    ports:
      - "8081:80"
    networks:
      - site-net

networks:
  site-net:
    driver: overlay
